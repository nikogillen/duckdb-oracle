.claude/
build/
.DS_Store

# DuckDB build tree (cloned locally for compilation, never committed)
duckdb/

# ODPI-C is fetched at build time by CMake (pinned version + SHA-256), not vendored
odpi/

# Local Oracle client / credentials — must never enter this public repo
.env*
*.ora
sqlnet.ora
tnsnames.ora
wallet*/
cwallet.sso
ewallet.p12*
*.pem
*.key
*.p12
*.jks
instantclient*/
*.dmg
